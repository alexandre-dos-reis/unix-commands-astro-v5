{
  "name": "unix-commands",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "docker:up": "docker-compose --env-file .env -f docker/unix-commands/compose.yml up -d",
    "docker:down": "docker-compose --env-file .env -f docker/unix-commands/compose.yml down",
    "db:migration:create": "pnpm exec prisma migrate dev",
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/tailwind": "^3.0.0",
    "@prisma/client": "^4.9.0",
    "astro": "^2.0.1",
    "tailwindcss": "^3.0.24"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "prisma": "^4.9.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}
